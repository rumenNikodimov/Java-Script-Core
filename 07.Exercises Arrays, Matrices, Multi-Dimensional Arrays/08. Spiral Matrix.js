function spiralMatrix(rows, cols) {
    let number = 1

    let target = rows * cols
    let matrix = fillMatrixZeros(rows, cows)

    function  fillMatrixZeros(rows , cows) {
        let matrix = []
        for (let i = 0; i < rows; i++) {
            let obj = rows[i];

        }
}
}